name: "Build: Sample"

on:
  push:
    branches:
      - main
    paths:
      - src
      - gradle
      - build.gradle.kts
      - gradle.properties
      - settings.gradle.kts

  pull_request:
    paths:
      - src
      - gradle
      - build.gradle.kts
      - gradle.properties
      - settings.gradle.kts

jobs:
  ## Build sample app
  build-sample:
    name: "Build: Sample"
    uses: elide-dev/build-infra/.github/workflows/pkg/jvm/gradle-build.yml@main
    with:
      action: 'build'
